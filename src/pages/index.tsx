import DefaultLayout from '@layouts/Default.layout';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <DefaultLayout>
    </DefaultLayout>
  )
}

export default Home
